{
  "name": "agentbin",
  "version": "0.1.0",
  "description": "Shareable, static-rendered pages for Agent session JSONL logs (Claude Code, CodeBuddy Code, Pi).",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "typescript": "^7.0.2"
  }
}
